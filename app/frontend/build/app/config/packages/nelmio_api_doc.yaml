nelmio_api_doc:
    documentation:
        info:
            title: Lolapi
            description: Lola's frontend API - Only accessible from Lolapy.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
