{% extends "dashboard/base/layout.html.twig" %}
{% block title %} {% trans %}dashboard.groupe.new.title{% endtrans %} {% endblock %}

{% block content %}

    <div class="breadcrumb d-flex justify-content-between align-items-center">
        <div class="d-flex">
            <i class="fas fa-users fa-2x sidebar-brand-icon"></i> 
            <span class="text-monospace sidebar-brand-text ml-4 mt-1">{% trans %}dashboard.groupe.new.title{% endtrans %} </span>
        </div>
        <a href="{{ path('dashboard_group_index') }}" class="btn btn-sm btn-primary">
            <i class="fas fa-arrow-left"></i>
            {% trans %}dashboard.groupe.new.btn_retour{% endtrans %}
        </a>
    </div>

    <div class="row">
        <div class="col-8">
            {{ include('dashboard/group/_form.html.twig', {'button_label': 'dashboard.groupe.new.btn_ajouter'|trans}) }}
        </div>
        <div class="col-4">
            <div class="card">
                <div class="card-header">
                    <i class="fas fa-info-circle"></i> {% trans %}dashboard.groupe.new.title{% endtrans %}
                </div>
                <div class="card-body">
                    <h5 class="card-title">{% trans %}dashboard.groupe.new.aide_titre{% endtrans %}</h5>
                    {% trans %}dashboard.groupe.new.aide{% endtrans %}
                </div>
            </div>                
        </div>
    </div>


{% endblock %}
